# GenerateCardmarketUrls
Java utility to automate Cardmarket URL generation and synchronization for LorcaHub cards. The tool fetches cards from the API, generates normalized Cardmarket URLs (including duplicate/version handling), skips already linked cards, and can update missing URLs automatically through the LorcaHub API.
